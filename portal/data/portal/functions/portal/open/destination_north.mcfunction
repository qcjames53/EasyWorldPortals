# Open the iz facing north. Clone the destination tiles to scratch with an appropriate offset.
# No inputs.
# Run as destination trigger entity at position.

tag @s add iz_destination
execute positioned ~ ~ ~-2 run clone ~-16 ~-1 ~-16 ~16 ~17 ~16 -16 -29 -16